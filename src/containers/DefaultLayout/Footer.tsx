import { Container } from "@mui/material";

// import { useTranslation } from "react-i18next";


const Footer = () => {
    // const { t } = useTranslation();

    return (
        <Container component="footer" sx={{ maxWidth: { xl: "xl", lg: "lg", md: "md" } }}>

        </Container>
    );
}

export default Footer;